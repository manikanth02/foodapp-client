specVersion: "v1beta"
serviceId: "foodapp-client"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "foodapp-client-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
